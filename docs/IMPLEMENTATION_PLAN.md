# 📋 План реализации отсутствующих требований

## Приоритет 1: Критические (блокирующие)

### 1.1 Frontend на Next.js (App Router)

**Цель**: Создать полноценный frontend с Next.js, TailwindCSS, Zustand, Apollo Client

#### Этапы:

**Шаг 1.1.1: Инициализация Next.js проекта**
```bash
# Создать папку client
mkdir client
cd client

# Инициализировать Next.js с TypeScript и App Router
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*"

# Установить зависимости
npm install @apollo/client graphql graphql-ws zustand react-hook-form @hookform/resolvers zod
npm install -D @types/node
```

**Шаг 1.1.2: Структура папок**
```
client/
├── app/                          # App Router
│   ├── layout.tsx               # Root layout
│   ├── page.tsx                 # Главная страница
│   ├── login/
│   │   └── page.tsx             # Страница входа
│   ├── register/
│   │   └── page.tsx             # Страница регистрации
│   ├── playlists/
│   │   ├── page.tsx            # Список плейлистов
│   │   └── [id]/
│   │       └── page.tsx         # Детали плейлиста
│   ├── playlists/new/
│   │   └── page.tsx             # Создание плейлиста
│   ├── songs/
│   │   └── page.tsx             # Список песен
│   └── profile/
│       └── page.tsx             # Профиль пользователя
├── components/                   # React компоненты
│   ├── ui/                      # Базовые UI компоненты
│   ├── playlist/                # Компоненты плейлистов
│   ├── song/                     # Компоненты песен
│   └── auth/                     # Компоненты авторизации
├── lib/                          # Утилиты
│   ├── apollo/                   # Apollo Client настройка
│   │   ├── client.ts            # HTTP клиент
│   │   ├── ws-client.ts         # WebSocket клиент
│   │   └── link.ts              # Apollo Link
│   ├── store/                    # Zustand stores
│   │   ├── auth-store.ts        # Store авторизации
│   │   └── playlist-store.ts    # Store плейлистов
│   └── utils/                    # Утилиты
├── hooks/                        # Custom hooks
│   ├── use-auth.ts              # Хук авторизации
│   └── use-subscription.ts      # Хук подписок
├── types/                        # TypeScript типы
│   └── graphql.ts               # GraphQL типы (сгенерированные)
├── public/                       # Статические файлы
├── .env.example                  # Пример переменных окружения
├── .eslintrc.json               # ESLint конфигурация
├── tailwind.config.ts           # Tailwind конфигурация
├── tsconfig.json                # TypeScript конфигурация
└── package.json
```

**Шаг 1.1.3: Настройка Apollo Client**
- Создать HTTP клиент для queries/mutations
- Настроить WebSocket клиент для subscriptions
- Настроить кэширование
- Добавить обработку ошибок

**Шаг 1.1.4: Настройка Zustand**
- Store для авторизации (token, user)
- Store для UI состояния
- Store для плейлистов (опционально)

**Шаг 1.1.5: Реализация экранов (минимум 5)**
1. **Главная страница** (`/`) - список публичных плейлистов
2. **Логин** (`/login`) - форма входа с валидацией
3. **Регистрация** (`/register`) - форма регистрации
4. **Список плейлистов** (`/playlists`) - мои плейлисты + создание
5. **Детали плейлиста** (`/playlists/[id]`) - просмотр, редактирование, добавление песен
6. **Профиль** (`/profile`) - информация о пользователе

**Шаг 1.1.6: Интеграция Subscriptions**
- Подключить WebSocket для реалтайм обновлений
- Реализовать обновление плейлистов в реальном времени
- Показывать уведомления о изменениях

**Шаг 1.1.7: Валидация форм**
- Использовать react-hook-form + zod
- Валидация на клиенте и сервере
- Показ ошибок валидации

**Время**: ~20-30 часов

---

### 1.2 Тесты (Jest)

**Цель**: Написать минимум 10 unit тестов + 1 интеграционный

#### Этапы:

**Шаг 1.2.1: Настройка Jest**
```bash
cd server
npm install -D @types/jest ts-jest jest-mongodb
```

Создать `jest.config.js`:
```javascript
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.ts', '**/?(*.)+(spec|test).ts'],
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/index.ts',
  ],
  coverageThreshold: {
    global: {
      branches: 70,
      functions: 70,
      lines: 70,
      statements: 70,
    },
  },
};
```

**Шаг 1.2.2: Unit тесты для резолверов (минимум 10)**

Создать `src/graphql/__tests__/resolvers.test.ts`:
- `register` mutation (успех, дубликат email, валидация)
- `login` mutation (успех, неверный пароль, пользователь не найден)
- `createPlaylist` mutation (успех, неавторизован)
- `addSongToPlaylist` mutation (успех, нет доступа)
- `me` query (успех, неавторизован)
- `playlists` query (фильтрация по доступу)
- `checkPlaylistAccess` helper (owner, contributor, forbidden)

**Шаг 1.2.3: Unit тесты для сервисов**

Создать `src/services/__tests__/pubsub.service.test.ts`:
- Тесты PubSubService (publish, subscribe)
- Тесты ConnectionManager (onConnect, onDisconnect)

**Шаг 1.2.4: Unit тесты для валидации**

Создать `src/validation/__tests__/validate.test.ts`:
- Тесты Zod схем (registerSchema, loginSchema, createPlaylistSchema)

**Шаг 1.2.5: Интеграционный тест**

Создать `src/__tests__/integration/graphql-api.test.ts`:
- Полный flow: регистрация → создание плейлиста → добавление песни
- Использовать in-memory MongoDB или test DB

**Время**: ~8-12 часов

---

### 1.3 Seed скрипт

**Цель**: Создать скрипт для заполнения БД тестовыми данными

#### Этапы:

**Шаг 1.3.1: Создать seed скрипт**

Создать `server/src/scripts/seed.ts`:
```typescript
// Создать 3-5 тестовых пользователей
// Создать 5-10 тестовых песен
// Создать 3-5 плейлистов
// Добавить песни в плейлисты
// Добавить контрибьюторов
```

**Шаг 1.3.2: Добавить скрипт в package.json**
```json
{
  "scripts": {
    "seed": "ts-node src/scripts/seed.ts"
  }
}
```

**Шаг 1.3.3: Обновить README**
- Добавить инструкцию по использованию seed скрипта

**Время**: ~2-3 часа

---

### 1.4 Dockerfile для Client

**Цель**: Создать Dockerfile для Next.js приложения

#### Этапы:

**Шаг 1.4.1: Создать Dockerfile**

Создать `client/Dockerfile`:
```dockerfile
FROM node:18-alpine AS base

# Install dependencies only when needed
FROM base AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

# Rebuild the source code only when needed
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

# Production image
FROM base AS runner
WORKDIR /app
ENV NODE_ENV production
COPY --from=builder /app/public ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static

EXPOSE 3000
ENV PORT 3000

CMD ["node", "server.js"]
```

**Шаг 1.4.2: Обновить docker-compose.yml**
```yaml
services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
    environment:
      - NEXT_PUBLIC_GRAPHQL_URL=http://server:4000/graphql
      - NEXT_PUBLIC_WS_URL=ws://server:4000/graphql
    networks:
      - app-network
```

**Шаг 1.4.3: Обновить next.config.js для standalone**
```javascript
module.exports = {
  output: 'standalone',
};
```

**Время**: ~2-3 часа

---

## Приоритет 2: Важные (не блокирующие)

### 2.1 Конфигурация ESLint и Prettier

**Цель**: Настроить линтер и форматтер

#### Этапы:

**Шаг 2.1.1: Создать .eslintrc.json для server**
```json
{
  "parser": "@typescript-eslint/parser",
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended"
  ],
  "rules": {
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/explicit-function-return-type": "off"
  }
}
```

**Шаг 2.1.2: Создать .prettierrc для server**
```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 100,
  "tabWidth": 2
}
```

**Шаг 2.1.3: Настроить для client (Next.js)**
- Использовать встроенную конфигурацию Next.js для ESLint
- Добавить .prettierrc

**Время**: ~1 час

---

### 2.2 Алиасы путей в TypeScript

**Цель**: Настроить алиасы для удобного импорта

#### Этапы:

**Шаг 2.2.1: Обновить tsconfig.json (server)**
```json
{
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "@/*": ["./*"],
      "@models/*": ["./models/*"],
      "@graphql/*": ["./graphql/*"],
      "@utils/*": ["./utils/*"],
      "@services/*": ["./services/*"]
    }
  }
}
```

**Шаг 2.2.2: Обновить tsconfig.json (client)**
- Next.js уже настраивает `@/*` по умолчанию
- Добавить дополнительные алиасы при необходимости

**Время**: ~30 минут

---

### 2.3 Документация

**Цель**: Улучшить README и добавить недостающую информацию

#### Этапы:

**Шаг 2.3.1: Обновить README.md**
- Добавить раздел "Как проверить реалтайм" с пошаговой инструкцией
- Добавить раздел "Демо" с ссылками (если есть продакшн)
- Добавить раздел "Тестовый пользователь" с credentials
- Добавить раздел "Команда" с ролями участников

**Шаг 2.3.2: Создать REALTIME_TESTING.md**
- Подробная инструкция по тестированию subscriptions
- Примеры запросов
- Скриншоты/видео (опционально)

**Время**: ~2-3 часа

---

## Приоритет 3: Опциональные (желательные)

### 3.1 Husky + lint-staged

**Цель**: Автоматическая проверка кода перед коммитом

#### Этапы:

**Шаг 3.1.1: Установить зависимости**
```bash
npm install -D husky lint-staged
```

**Шаг 3.1.2: Настроить husky**
```bash
npx husky install
npx husky add .husky/pre-commit "npx lint-staged"
```

**Шаг 3.1.3: Настроить lint-staged в package.json**
```json
{
  "lint-staged": {
    "*.ts": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

**Время**: ~30 минут

---

### 3.2 Генерация GraphQL типов

**Цель**: Автоматическая генерация TypeScript типов из GraphQL схемы

#### Этапы:

**Шаг 3.2.1: Установить GraphQL Code Generator**
```bash
npm install -D @graphql-codegen/cli @graphql-codegen/typescript @graphql-codegen/typescript-operations
```

**Шаг 3.2.2: Создать codegen.yml**
```yaml
schema: http://localhost:4000/graphql
documents: 'src/**/*.graphql'
generates:
  src/types/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
```

**Шаг 3.2.3: Добавить скрипт в package.json**
```json
{
  "scripts": {
    "codegen": "graphql-codegen --config codegen.yml"
  }
}
```

**Время**: ~1 час

---

## 📊 Общая оценка времени

| Задача | Время |
|--------|-------|
| Frontend (Next.js) | 20-30 часов |
| Тесты (Jest) | 8-12 часов |
| Seed скрипт | 2-3 часа |
| Dockerfile для client | 2-3 часа |
| ESLint/Prettier | 1 час |
| Алиасы путей | 30 минут |
| Документация | 2-3 часа |
| Husky + lint-staged | 30 минут |
| GraphQL Codegen | 1 час |
| **ИТОГО** | **37-53 часа** |

## 🎯 Рекомендуемый порядок выполнения

1. **Неделя 1**: Frontend (Next.js) - базовая структура и основные экраны
2. **Неделя 2**: Frontend - интеграция Apollo Client и Subscriptions
3. **Неделя 3**: Тесты (Jest) - unit и интеграционные
4. **Неделя 4**: Docker, Seed скрипт, документация, финальная полировка

## ✅ Чеклист готовности

- [ ] Frontend на Next.js создан и работает
- [ ] Минимум 5 экранов реализованы
- [ ] Apollo Client настроен с subscriptions
- [ ] Zustand stores работают
- [ ] Минимум 10 unit тестов написано
- [ ] 1 интеграционный тест написан
- [ ] Seed скрипт создан и работает
- [ ] Dockerfile для client создан
- [ ] docker-compose.yml обновлен
- [ ] ESLint и Prettier настроены
- [ ] Алиасы путей настроены
- [ ] README обновлен с инструкциями
- [ ] Реалтайм протестирован и задокументирован

---

**Примечание**: Этот план можно адаптировать в зависимости от приоритетов и доступного времени.
